import React from "react"
import { SafeAreaView, Text } from "react-native"

const Order = (props) => {
    return (
        <SafeAreaView>
            <Text>SCREEN</Text>
        </SafeAreaView>
    )
}

export default Order;